% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsm_trips.R
\name{fsm_zone_from_trips}
\alias{fsm_zone_from_trips}
\title{Derive a Zone Set from Trip Records}
\usage{
fsm_zone_from_trips(
  data,
  origins = "origin",
  destinations = "destination",
  weights = NULL,
  modes = NULL,
  mode = "mode",
  include = NULL
)
}
\arguments{
\item{data}{A data.frame, data.table, list, or \code{fsm_population}
containing trip records.}

\item{origins}{Character string naming the origin-zone column.}

\item{destinations}{Character string naming the destination-zone column.}

\item{weights}{Optional character string naming a finite, non-negative
numeric trip-weight column. For an \code{fsm_population}, the default uses
\code{population_count}; for other inputs, each row has weight one.}

\item{modes}{Optional vector of mode values to retain before aggregation.
A value matches either a complete mode-chain label or any component of a
chain separated by \code{+}; for example, \code{"transit"} matches both
\code{"transit"} and \code{"walk+transit"}. Multiple values use OR
matching. If \code{NULL}, all modes are included.}

\item{mode}{Character string naming the mode column. It is required only
when \code{modes} is not \code{NULL}.}

\item{include}{Optional vector of additional zone identifiers, such as zones
with no observed trips.}
}
\value{
An \code{fsm_zone} object containing a \code{zone_id} column.
}
\description{
Extracts the distinct origin and destination identifiers from
trip records and returns a minimal \code{fsm_zone} object. Zones with no
observed trips cannot be inferred from trip records; supply their identifiers
through \code{include} when they are known.
}
\examples{
fsm_zone_from_trips(fsm_toy_population)
fsm_zone_from_trips(fsm_toy_population, include = 7L)
}
