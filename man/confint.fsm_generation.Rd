% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsm_inspect.R
\name{confint.fsm_generation}
\alias{confint.fsm_generation}
\title{Confidence Intervals for a Generation Regression}
\usage{
\method{confint}{fsm_generation}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{A regression-based \code{fsm_generation} object.}

\item{parm}{A specification of parameters for which confidence intervals are
requested. Passed to \code{stats::confint()}.}

\item{level}{Confidence level. Passed to \code{stats::confint()}.}

\item{...}{Additional arguments passed to \code{stats::confint()}.}
}
\value{
A named list of confidence-interval matrices, one for each fitted
outcome.
}
\description{
Computes coefficient confidence intervals for each fitted
regression outcome in an \code{fsm_generation} object. The result is a
named list containing a matrix for production, attraction, or both. Methods
without fitted regression coefficients, including cross-classification,
growth-factor, and custom models, do not provide confidence intervals.
}
\examples{
totals <- fsm_od_totals(fsm_toy_od, fsm_toy_zone)
fit <- fsm_generation(
  fsm_toy_zone,
  totals,
  method = "regression",
  production_formula = production ~ 0 + population,
  attraction_formula = attraction ~ 0 + jobs
)
confint(fit)
}
