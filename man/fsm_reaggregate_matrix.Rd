% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsm_matrix.R
\name{fsm_reaggregate_matrix}
\alias{fsm_reaggregate_matrix}
\title{Re-aggregate a Matrix to a Different Zone System}
\usage{
fsm_reaggregate_matrix(
  x,
  row_map,
  col_map = row_map,
  from = NULL,
  to = NULL,
  col_from = from,
  col_to = to
)
}
\arguments{
\item{x}{A numeric matrix with unique row and column names.}

\item{row_map}{A data.frame or data.table with two columns describing the
row correspondence.}

\item{col_map}{Optional correspondence table for the columns. If omitted,
the same mapping used for rows is also applied to columns.}

\item{from}{Optional name of the column in the correspondence table
containing the original identifiers. Defaults to the first column.}

\item{to}{Optional name of the column in the correspondence table containing
the target identifiers. Defaults to the second column.}

\item{col_from}{Optional source-identifier column in \code{col_map}. Defaults
to \code{from}.}

\item{col_to}{Optional target-identifier column in \code{col_map}. Defaults
to \code{to}.}
}
\value{
A numeric matrix aggregated to the target row and column systems.
}
\description{
Re-aggregates a numeric matrix by summing values according to
correspondence tables. This is useful when mapping blocks to zones, zones to
districts, or any other many-to-one spatial classification. Row and column
mappings may differ, and target identifiers may be numeric or character.
}
\examples{
m <- matrix(
  c(0, 1, 2, 3,
    4, 0, 5, 6,
    7, 8, 0, 9,
    1, 2, 3, 0),
  nrow = 4,
  byrow = TRUE
)
rownames(m) <- colnames(m) <- c("a", "b", "c", "d")

map <- data.frame(
  old = c("a", "b", "c", "d"),
  new = c("x", "x", "y", "y")
)

fsm_reaggregate_matrix(m, map)
}
\references{
Cascetta, E. (2009). \emph{Transportation Systems Analysis: Models and
Applications}. Springer. See Sect. 1.3.1 for zoning and Sect. 4.3.1.2 for
properties related to zonal aggregation.
}
